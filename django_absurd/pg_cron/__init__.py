"""Opt-in pg_cron scheduler app for django-absurd.

Add ``"django_absurd.pg_cron"`` to ``INSTALLED_APPS`` (after ``"django_absurd"``)
to schedule declared ``SCHEDULE`` entries database-side via the pg_cron extension.
"""
