[pytest]
DJANGO_SETTINGS_MODULE = "benchmarks.settings"
addopts = [
  "--reuse-db",
  "--strict-markers",
]
pythonpath = [".."]
testpaths = ["tests"]
# Real workers on real time; the whole smoke is well under this.
timeout = "120"
