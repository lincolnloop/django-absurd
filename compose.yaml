---
services:
  db:
    environment:
      - POSTGRES_PASSWORD=postgres
    image: postgres:16-alpine
    ports:
      - "${PGPORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data/

volumes:
  pgdata:
