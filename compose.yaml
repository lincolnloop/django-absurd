---
services:
  db:
    environment:
      - POSTGRES_PASSWORD=postgres
    image: postgres:18-alpine
    ports:
      - "${PGPORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
