[project]
dependencies = [
  "django-absurd",
  "django==6.0.6",
  "psycopg[binary]==3.3.4",
]
name = "django-absurd-example"
requires-python = ">=3.14"
version = "0"

[tool.uv.sources]
django-absurd = {path = "..", editable = true}
